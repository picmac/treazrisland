# Backend environment configuration template
# Copy to backend/.env and adjust values for your environment.

# General application settings
NODE_ENV=development
PORT=4000

# Database connection string for application persistence
DATABASE_URL=postgresql://treazr:treazr@localhost:5432/treazr

# Redis instance for caching and queues
REDIS_URL=redis://localhost:6379/0

# MinIO object storage credentials and endpoint
STORAGE_ENDPOINT=http://localhost:9000
STORAGE_BUCKET=treazr-assets
STORAGE_ACCESS_KEY=treazr
STORAGE_SECRET_KEY=treazr_secret

# LocalStack emulator bridge for AWS-compatible services
EMULATOR_HOST=localhost
EMULATOR_PORT=4566

# Pixellab.ai integration token used for art generation workflows
PIXELLAB_API_TOKEN=replace-with-pixellab-api-token
