# Frontend environment configuration template for Vite
# Copy to frontend/.env.local (or .env) and adjust values for your environment.

# Base URL where the frontend calls the backend API gateway
VITE_API_BASE_URL=http://localhost:4000

# URLs that proxy database or cache health information surfaced by the backend
VITE_STATUS_PAGE_URL=http://localhost:4000/status

# MinIO asset delivery configuration surfaced in the UI
VITE_STORAGE_ENDPOINT=http://localhost:9000
VITE_STORAGE_BUCKET=treazr-assets

# Pixellab.ai token for client-side preview tooling (never commit production secrets)
VITE_PIXELLAB_PREVIEW_TOKEN=replace-with-pixellab-preview-token
