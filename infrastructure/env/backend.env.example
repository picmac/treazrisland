# Backend environment configuration template
# Copy to backend/.env and adjust values for local development.

NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://treazr:treazr@postgres:5432/treazr
