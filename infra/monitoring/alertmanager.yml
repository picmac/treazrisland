route:
  receiver: oncall
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: oncall
    webhook_configs:
      - url: http://backend:3001/internal/alerts
        send_resolved: true
