# Copy this file to infra/monitoring/secrets/metrics_token and replace the value with the METRICS_TOKEN from backend.env.
# This placeholder keeps Docker Compose bootable in CI and local dev, but it must be overridden for real deployments.
TREAZRISLAND_METRICS_TOKEN_PLACEHOLDER
